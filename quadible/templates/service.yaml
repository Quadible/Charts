apiVersion: v1
kind: Service
metadata:
  name: {{ include "quadible.fullname" . }}
  labels:
    {{- include "quadible.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quadible.selectorLabels" . | nindent 4 }}
